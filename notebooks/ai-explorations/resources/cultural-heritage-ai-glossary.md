# Cultural Heritage Glossary

A glossary of some (in no way attempting to be compresensive) terms and how
they are being used in this site.

## Cultural Heritage Object

## Object 

Some set of pixels isolatable in a digital image from the rest of the image 
which is an object class of any kind (tree, cat, bike, etc). 

## Annotations

Sometimes refers to area of an image that have been labelled as containing some *Object*.  Used as part of model training so it can learn to label new images with any objects from its training set

Also known as: bounding box, pixel level, localised

[If I could make an appeal to the world, please stop using the word 
annotations for a while, it's already used in so many domains with so many clashing 
meanings, we don't need any more ]

## Neural-network Setup

  * Training data
  * Architecture
  * 

## (neural-network) Architecture

Neural network architecture is composed of a series of layers
  * Training data
  * Model Architecture

    
## Classes / Labels

## Data Augmentation

Generating more training data by creating artifical variants from the "real" training data

## Transfer Learning

## Image Segmentation

## Instance Level Recognition

Identify a particular instance in an image of a class of objects

### Category Level Recognition

Identify a category/class within an image (but not a particular instance of the category/class)

## Large Language Models (LLM)

## Vision Language Models  (VLM)

## Mixed-Modal Models

## Zero Shot

Identify a class/label in test data without it having appeared in training data

## Models

### Faster-RCNN

Used as object detector in images

### BERT

Natural language processing

Also known as: multi-model

### Solution Ranking

It would obvisouly be helpful if for each AI cultural heritage challenge there was some clear test set that could be used to compare
an approach to allow tracking of progress over time. However in most cases each paper published develops their own data set for testing. Some comparisions are given. Too early to have a settled test set. In addition, some challenges are particular to sub-sets
of cultural heritage (e.g. paintings versus vases). Perhaps a useful approach would be a dataset...

